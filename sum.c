#include <stdio.h>

int main() {
    int a, b, sum;

    printf("Enter first numbers: ");
    printf("Enter second numbers: ");
    scanf("%d %d", &a, &b);
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("Sum = %d", sum);

    return 0;
}